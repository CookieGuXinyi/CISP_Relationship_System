# CISP 血缘质量溯源系统 — 实习汇报文档

---

## 一、项目背景与目标

### 1.1 项目背景

在数据仓库（Data Warehouse）治理领域，**数据血缘（Data Lineage）** 是追踪数据从源头到终点的流转路径的关键技术。监管报送场景下，监管机构（如 CISP）要求金融机构对报送数据的每一个字段提供完整的溯源链路，证明数据的来源、加工逻辑和合规性。

当前机构面临的痛点：

- **血缘缺失**：大量历史任务未建立血缘关系，报送数据无法溯源
- **手工追溯**：依赖人工阅读 SQL 脚本逐行分析字段来源，效率极低
- **质量盲区**：缺乏可视化手段快速识别数据流向和依赖关系

### 1.2 项目目标

构建一套 **CISP 血缘质量溯源系统**，实现：

1. **SQL 自动解析**：自动提取 Hive/Spark SQL 中的字段级血缘关系
2. **血缘持久化存储**：将解析结果结构化存储，支持多任务、多层级管理
3. **可视化血缘展示**：通过力导向图/分层布局图直观展示表级/字段级血缘
4. **多节点查询**：支持指定表/字段的上下游多层级血缘追溯
5. **批量任务分析**：从生产环境批量拉取历史 SQL 并解析血缘

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端展示层                             │
│          Vue 3 + Element Plus + ECharts                     │
│    (SQL输入 / 统计卡片 / 双区域血缘展示 / 节点详情弹窗)        │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP / REST API
┌──────────────────────────▼──────────────────────────────────┐
│                        后端服务层                             │
│                    Node.js + Express                         │
│  (API路由 / BFS多层查询 / 批量分析 / 进度轮询)                 │
└──────────────┬───────────────────────────┬──────────────────┘
               │                           │
┌──────────────▼──────────┐  ┌────────────▼───────────────────┐
│     SQL 解析引擎         │  │      数据存储层                 │
│  Python + sqlglot       │  │   SQLite (血缘库)               │
│  (词法/语法分析 /        │  │   PostgreSQL (生产任务元数据)    │
│   字段级血缘提取)        │  │                                │
└─────────────────────────┘  └────────────────────────────────┘
```

### 2.2 技术栈

| 层级       | 技术              | 选型理由                                   |
| ---------- | ----------------- | ------------------------------------------ |
| 前端框架   | Vue 3 (CDN)       | 轻量、响应式、无需构建工具                 |
| UI 组件库  | Element Plus      | 企业级组件丰富，中文文档完善               |
| 可视化图表 | ECharts           | 原生支持力导向图/分层布局，交互性强        |
| 后端框架   | Node.js + Express | I/O 密集型任务（SQL 解析调度、数据库查询） |
| SQL 解析   | Python + sqlglot  | 专业 SQL 解析库，支持 Hive/Spark 方言      |
| 数据库     | SQLite            | 轻量嵌入式，零配置，适合 Demo 演示         |
| 外部数据源 | PostgreSQL        | 生产环境任务元数据存储（只读）             |

### 2.3 数据模型

```
field_lineage 表（核心血缘表）
├── id              主键
├── source_table    源表名
├── source_field    源字段名
├── target_table    目标表名
├── target_field    目标字段名
├── expression      加工表达式
├── full_expression 完整表达式（含表名）
├── expression_type 表达式类型（direct/agg_sum/filter 等）
├── source_role     源角色（direct/data_source/filter）
├── job_id          任务ID
├── report_code     报送代码（如 Y4）
├── layer           数据层级（COLLECT/BASE/RESULT）
└── created_at      创建时间
```

---

## 三、核心功能实现

### 3.1 SQL 解析引擎

**核心流程**：

```
SQL文本 → 预处理(去注释/参数替换) → 分词 → 语法解析(AST) → 遍历AST提取血缘
```

**关键实现**：

- **多语句分割**：支持包含 `INSERT`、`SELECT`、`CTE`、`CREATE TABLE` 等多语句的 SQL 脚本
- **注释处理**：自定义注释剥离器，正确处理字符串内的 `--` 和 `/* */`
- **血缘提取**：从 AST 的 `SELECT` 子句出发，追踪每个目标字段的表达式树，识别源表和源字段
- **表达式分类**：识别 `direct`（直接映射）、`agg_sum`（聚合求和）、`filter`（筛选条件）等加工类型
- **表级血缘推断**：从字段级血缘反推表级依赖关系，生成 `TABLE_LEVEL` 虚拟节点

**支持的 SQL 方言**：

- Hive SQL、Spark SQL、MySQL、PostgreSQL

### 3.2 血缘存储与查询

**存储设计**：

- 按 `job_id` 隔离：同一任务重解析时自动覆盖旧数据
- 同时存储字段级（精确到字段）和表级（`source_field=TABLE_LEVEL`）血缘
- 数据层级自动推断：根据表名关键字（ODS/DWD/DWS/ADS）自动归类

**多层级血缘查询（BFS 算法）**：

```
输入：目标表/字段 + 方向(upstream/downstream/both) + 最大层级
算法：BFS 广度优先搜索
  - downstream: 从源表沿 source_table→target_table 方向递归
  - upstream:  从源表沿 target_table→source_table 方向递归
  - both:      拆分为两次独立 BFS，避免交叉污染
输出：带层级标记的血缘关系列表
```

**核心数据结构**：

- `upstreamMap`: `表 → 以该表为 source_table 的所有行`（用于下游遍历）
- `downstreamMap`: `表 → 以该表为 target_table 的所有行`（用于上游遍历）
- `visitedRows`: 去重集合，防止 BFS 中重复边

### 3.3 可视化展示

**双区域独立展示**：

| 区域                   | 功能定位     | 默认布局   | 数据源          |
| ---------------------- | ------------ | ---------- | --------------- |
| 区域1：血缘关系总览    | 全局视角查看 | 力导向图   | 全量/筛选后血缘 |
| 区域2：表/字段血缘查询 | 精准溯源     | 分层布局图 | API 查询结果    |

**力导向图**：

- 节点表示表，边表示血缘方向
- 支持拖拽、缩放、点击查看详情
- 不同层级用不同颜色标识（蓝=采集层/绿=基础层/橙=结果层）

**分层布局图**：

- 按 BFS 层级从左到右排列，数据流向直观
- 根表在左，目标表在右
- 动态间距调整，层级越多间距越小

**交互功能**：

- 点击节点 → 弹窗显示该表的字段级血缘详情
- 切换视图模式（图形/表格）
- 按层级、角色、最少字段数筛选

### 3.4 批量任务分析

**流程**：

1. 从 PostgreSQL 读取生产环境任务列表
2. 逐个拉取任务 SQL 语句
3. 调用 Python 引擎解析并存储血缘
4. 实时进度反馈（轮询模式）

**进度管理**：

- 状态机：`idle → running → completed/failed`
- 实时显示：总任务数、成功数、失败数、当前任务
- 支持中途取消

---

## 四、技术亮点与踩坑记录

### 4.1 技术亮点

| 序号 | 亮点             | 说明                                                                   |
| ---- | ---------------- | ---------------------------------------------------------------------- |
| 1    | 字段级血缘提取   | 不仅识别表级依赖，精确到字段级，支持聚合、筛选、CASE WHEN 等复杂表达式 |
| 2    | 多层级 BFS 查询  | 支持指定表的 N 层上下游追溯，`both` 模式拆分为独立 BFS 避免交叉污染  |
| 3    | 双区域独立管理   | 总览与搜索使用独立的数据源、图表实例、视图状态，互不干扰               |
| 4    | 数据层级自动推断 | 根据表名命名规范自动归类到采集层/基础层/结果层                         |
| 5    | 实时进度反馈     | 批量分析采用前端轮询 + 后端内存状态的异步通信模式                      |
| 6    | 前端自适应布局   | 分层布局根据层级数量动态调整间距，防止溢出                             |

### 4.2 踩坑与解决方案

#### 坑1：`both` 方向 BFS 交叉污染

**现象**：查询 A 表上下游时，下游 B 表的其他上游表（与 A 无关的）也被包含进来。

**原因**：原 BFS 在 `both` 模式下同时向上游和下游扩展，导致从 A → B 后又从 B 向上游扩展，把 B 的所有上游表都拉了进来。

**解决方案**：将 `both` 模式拆分为两次独立的单向 BFS：

```javascript
// 下游 BFS：只沿 source→target 方向
bfs(targetTable, downstreamEdges, maxLevel, 0);
// 上游 BFS：只沿 target→source 方向
bfs(targetTable, upstreamEdges, maxLevel, 0);
```

#### 坑2：分层布局节点只显示在右侧

**现象**：切换分层布局后，所有节点集中在容器右侧约 1/8 区域。

**原因**：ECharts 的 `layout: 'none'` 模式以容器中心为坐标原点，根表（level 0）放在 x=0，高层级节点在右侧正坐标区域。

**解决方案**：计算 x 偏移量将整体居中：

```javascript
const xOffset = -(maxLevel * xSpacing) / 2;
nodes.forEach(n => { n.x = n.level * xSpacing + xOffset; });
```

#### 坑3：从表格视图切回图谱后无法渲染

**现象**：首次搜索图谱正常，切换到表格再切回图谱后，图形空白。

**原因**：`v-if` 切换导致 DOM 销毁重建，ECharts 实例引用的是已销毁的 DOM 节点。

**解决方案**：在 `watch(searchViewMode)` 中重新初始化图表实例：

```javascript
watch(searchViewMode, () => {
  if (searchViewMode.value === 'graph') {
    nextTick(() => {
      nextTick(() => {  // 双重 nextTick 确保 DOM 完全渲染
        chartInstance.dispose();
        chartInstance = echarts.init(newContainer);
        updateSearchGraph();
      });
    });
  }
});
```

#### 坑4：搜索区域点击节点显示全库数据

**现象**：搜索模式下点击节点，弹窗显示的字段数与图谱中不一致（显示全库数据）。

**原因**：`onNodeClick` 回调始终使用 `allLineage.value` 而非当前图表对应的数据源。

**解决方案**：通过 DOM 容器判断点击来源，选择正确的数据源：

```javascript
const clickedEl = params.event?.event?.target;
if (graphRef2.value.contains(clickedEl) || graphRef3.value.contains(clickedEl)) {
  sourceData = searchLineage.value;  // 搜索区域用搜索数据
} else {
  sourceData = displayLineage.value; // 总览区域用筛选数据
}
```

#### 坑5：大数据量渲染卡顿

**现象**：加载 5000 条血缘数据时页面无响应。

**原因**：数百个节点 + 上千条边的力导向图计算压力过大。

**解决方案**：

- 后端：将 API 默认 `maxRows` 从 5000 降到 2000
- 前端：添加节点数检查，超过 80 个表节点时提示用户缩小范围
- 分层布局：限制 BFS 深度和返回条数

---

## 五、演示数据

### 5.1 功能演示清单

| 序号 | 演示项       | 操作步骤                                     | 预期效果                             |
| ---- | ------------ | -------------------------------------------- | ------------------------------------ |
| 1    | SQL 解析     | 粘贴 SQL → 选择方言 → 点击「解析并存储」   | 显示统计卡片：源表数/目标表数/血缘数 |
| 2    | 总览力导向图 | 加载血缘 → 点击「图形视图」                 | 显示全量表级血缘关系图               |
| 3    | 总览筛选     | 切换层级/角色/最少字段数 → 点击「重新加载」 | 图谱按筛选条件更新                   |
| 4    | 字段详情     | 点击图谱中任意节点                           | 弹窗显示该表的字段级血缘             |
| 5    | 表搜索       | 区域2 选择表 → 选择方向 → 查询             | 分层布局显示该表的上下游血缘         |
| 6    | 字段搜索     | 区域2 输入字段名 → 查询                     | 显示涉及该字段的所有表的血缘         |
| 7    | 布局切换     | 切换力导向图/分层布局                        | 布局方式切换，数据不变               |
| 8    | 视图切换     | 图形视图 ↔ 表格视图                         | 两种视图正确切换，数据一致           |
| 9    | 批量分析     | 点击「从数据库批量分析」                     | 进度条实时更新，显示结果统计         |

### 5.2 关键 SQL 示例

```sql
-- 演示用 SQL：多表 JOIN + 聚合
INSERT OVERWRITE TABLE rmc.ads_tb_zsyyb_month_report PARTITION(period='202606')
SELECT
    a.branch_code,
    a.sec_type,
    SUM(CASE WHEN a.biz_date BETWEEN '20260601' AND '20260630' 
        THEN a.cjje ELSE 0 END) AS match_sum,
    COALESCE(b.match_sum, 0) + COALESCE(c.match_sum_rzrq, 0) AS total_match
FROM rmc.dw_tb_tzqjytj a
LEFT JOIN rmc.dim_branch b ON a.branch_code = b.code
LEFT JOIN rmc.dim_product c ON a.sec_type = c.type
WHERE a.market_code IN ('SH', 'SZ')
GROUP BY a.branch_code, a.sec_type;
```

---

## 六、成果与总结

### 6.1 功能清单

| 模块     | 功能点                               | 完成状态 |
| -------- | ------------------------------------ | -------- |
| SQL 解析 | Hive/Spark/MySQL/PostgreSQL 方言支持 | ✅       |
| SQL 解析 | 多语句分割与解析                     | ✅       |
| SQL 解析 | 字段级血缘提取（含表达式分类）       | ✅       |
| SQL 解析 | 表级血缘自动推断                     | ✅       |
| 数据存储 | SQLite 持久化存储                    | ✅       |
| 数据存储 | 任务级隔离与覆盖更新                 | ✅       |
| 数据存储 | 层级自动推断                         | ✅       |
| 血缘查询 | 全量血缘加载与筛选                   | ✅       |
| 血缘查询 | 多层级 BFS 上下游查询                | ✅       |
| 血缘查询 | 字段级精准查询                       | ✅       |
| 可视化   | 力导向图（总览）                     | ✅       |
| 可视化   | 分层布局图（搜索）                   | ✅       |
| 可视化   | 图形/表格双视图切换                  | ✅       |
| 可视化   | 节点点击字段详情弹窗                 | ✅       |
| 批量分析 | PostgreSQL 任务批量拉取与解析        | ✅       |
| 批量分析 | 实时进度反馈                         | ✅       |

### 6.2 技术收获

1. **SQL 解析原理**：深入理解词法分析、语法分析、AST 遍历的完整流程
2. **数据血缘技术**：掌握字段级血缘提取、多层级血缘查询的核心算法
3. **前端可视化**：学习 ECharts 力导向图/分层布局的配置与交互设计
4. **前后端分离架构**：实践 RESTful API 设计、状态管理、双区域独立渲染
5. **调试与排错**：积累组件生命周期、DOM 操作、内存管理等实战经验

### 6.3 后续优化方向

| 优先级 | 方向              | 说明                                   |
| ------ | ----------------- | -------------------------------------- |
| 高     | 血缘导出功能      | 支持导出血缘关系为 Excel/JSON          |
| 高     | 血缘比对          | 两次解析结果的差异对比（血缘变更检测） |
| 中     | 更多 SQL 语法支持 | 窗口函数、物化视图、存储过程           |
| 中     | 血缘影响分析      | 上游变更时评估下游受影响范围           |
| 中     | 质量规则集成      | 将血缘与质量规则联动，自动校验         |
| 低     | 生产部署          | Docker 化、Nginx 反向代理、HTTPS       |
| 低     | 用户权限          | 多用户、角色权限控制                   |

---

## 七、附录

### A. 项目文件结构

```
cisp_demo/
├── server.js              # 后端服务主入口
├── parse_sql_glot.py      # SQL 解析引擎（Python + sqlglot）
├── parse_sql.py           # SQL 解析引擎（旧版，仅作参考）
├── init-db.js             # 数据库初始化脚本
├── cisp.db                # SQLite 数据库文件
├── testSQL.txt            # 测试 SQL 脚本
├── TODO.md                # 已知问题记录
├── package.json           # Node.js 依赖配置
└── frontend/
    ├── index.html         # 前端页面（Vue 3 + Element Plus + ECharts）
    ├── package.json       # 前端依赖配置
    └── package-lock.json  # 依赖锁定文件
```

### B. 启动方式

```bash
# 1. 安装依赖
npm install

# 2. 启动服务
node server.js

# 3. 访问
# 浏览器打开 http://localhost:3000
```

### C. API 接口列表

| 接口                    | 方法 | 说明                         |
| ----------------------- | ---- | ---------------------------- |
| `/api/parse-sql`      | POST | 解析 SQL 并存储血缘          |
| `/api/lineage`        | GET  | 查询全量血缘（支持筛选参数） |
| `/api/lineage/multi`  | GET  | 多层级血缘查询               |
| `/api/tables`         | GET  | 获取所有表列表               |
| `/api/batch/analyze`  | POST | 启动批量分析任务             |
| `/api/batch/progress` | GET  | 查询批量分析进度             |
| `/api/stats`          | GET  | 获取统计数据                 |

### D. 关键依赖版本

| 依赖         | 版本   | 用途              |
| ------------ | ------ | ----------------- |
| Node.js      | ≥16   | 运行时环境        |
| Express      | 4.x    | Web 框架          |
| sqlite3      | 5.x    | SQLite 驱动       |
| pg           | 8.x    | PostgreSQL 驱动   |
| Python       | 3.x    | SQL 解析引擎      |
| sqlglot      | latest | SQL 解析库        |
| Vue          | 3.x    | 前端框架（CDN）   |
| Element Plus | 2.x    | UI 组件库（CDN）  |
| ECharts      | 5.x    | 可视化图表（CDN） |
